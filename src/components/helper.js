/**Function to add 2 numbers */
const sum = (a, b) => a + b;
const diff = (a, b) => a - b;
const multi = (a, b) => a * b;

const NUMBER_METHODS = {
  sum,
  diff,
  multi,
};

export default NUMBER_METHODS;
